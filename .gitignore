# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr

# Comprometer dist del frontend Y del backend al repo (server necesita ambos sin compilar)
!/dist
!/server/dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/.agents
/.claude

# Config local — cada entorno tiene el suyo, no se sobreescribe con git pull
server/ecosystem.config.cjs

# Variables de entorno — contienen credenciales (DB, JWT) y no se suben al repo
.env
.env.*
!.env.example
!server/.env.example

# Respaldos de migración (JSONs grandes, solo para uso local)
server/backups/

.lovable
.agents
.claude